<?xml version="1.0"?>
<!--
  ClickHouse Keeper standalone config. Runs as a separate container
  (see docker-compose.replicated.yml) to provide Raft consensus for
  ReplicatedMergeTree across the two shard nodes.

  Single-keeper is fine for dev/staging. For real HA, deploy 3 keeper
  nodes across failure domains — comment "server-2" and "server-3"
  below are templates.
-->
<clickhouse>
    <logger>
        <level>information</level>
        <console>true</console>
    </logger>

    <listen_host>0.0.0.0</listen_host>

    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse-keeper/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse-keeper/coordination/snapshots</snapshot_storage_path>

        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>information</raft_logs_level>
        </coordination_settings>

        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>keeper</hostname>
                <port>9234</port>
            </server>
            <!--
            For 3-node quorum, add:
            <server><id>2</id><hostname>keeper-2</hostname><port>9234</port></server>
            <server><id>3</id><hostname>keeper-3</hostname><port>9234</port></server>
            and scale the `keeper` compose service accordingly.
            -->
        </raft_configuration>
    </keeper_server>
</clickhouse>
